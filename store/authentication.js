const state = () => ({
  showLoader: Boolean,

});

const mutations = {


}
const actions = {
  async logout({ commit }) {}
}
const getters = {
  
}

export default {
  namespaced: false,
  state,
  mutations,
  actions,
  getters
}